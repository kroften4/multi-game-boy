CompileFlags:
  Remove: [
    # was used for esp v5.5, seems to be fixed in v6.0
    # -mlongcalls,
    # -mlong-calls,
    # '-fno-shrink-wrap',
    # '-fstrict-volatile-bitfields',
    # '-fno-tree-switch-conversion',
    # '-fzero-init-padding-bits=all',
    # '-fno-malloc-dce'
  ]
